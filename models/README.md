# Models Directory

This directory is reserved for future trained model artifacts, checkpoints, experiment exports, and explainability assets.

Generated model artifacts are intentionally excluded from git. Future prompts can add model registry metadata or artifact loading conventions without changing the top-level project layout.

